import storageSpec from '../../../utils/storage-spec';
import { module, test } from 'qunit';

module('Unit | Utility | storage spec');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = storageSpec();
  assert.ok(result);
});
