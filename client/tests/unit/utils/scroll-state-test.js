import scrollState from '../../../utils/scroll-state';
import { module, test } from 'qunit';

module('Unit | Utility | scroll state');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scrollState();
  assert.ok(result);
});
