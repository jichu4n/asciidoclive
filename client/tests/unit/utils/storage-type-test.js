import storageType from '../../../utils/storage-type';
import { module, test } from 'qunit';

module('Unit | Utility | storage type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = storageType();
  assert.ok(result);
});
